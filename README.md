# CSC151-a4
Assignment4 Cipher Test
This the test code for asignment3-encipher and decipher message.
As you run the main test (caeser.py), the result is generated as a txt file in the folder which is the decipher message from encrypted-message.txt.
For the test.py, you can run it in terminal with operation "pytest test.py" and the result will show that all three functions passed like "test.py .... "
